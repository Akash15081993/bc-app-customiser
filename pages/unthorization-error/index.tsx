import { Flex, Panel } from "@bigcommerce/big-design";

const Index = () => {
    return (
        <Panel>
            <Flex justifyContent={'center'} alignItems={'center'} style={{minHeight:'300px'}}>
                <h3 style={{textAlign:'center', margin:'0s'}}>403 | Empty sign and authorizations</h3>
            </Flex>
        </Panel>
    );
};

export default Index;
