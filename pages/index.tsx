import { Flex, H4, Panel } from '@bigcommerce/big-design';

const Index = () => {
    return (
        <Panel header="Homepage" id="home">
            <Flex>
                <H4>Product customizer builder</H4>
            </Flex>
        </Panel>
    );
};

export default Index;
